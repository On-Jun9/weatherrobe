# Security hardening
# Disable lifecycle scripts by default (Shai-Hulud worm mitigation)
ignore-scripts=true
# Enforce exact versions on install
save-exact=true
# Strict SSL
strict-ssl=true
# Verify package signatures
audit=true
